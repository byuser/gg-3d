# dependencies + build output (the built bundle is produced in CI / deploy)
node_modules/
dist/

# local env (e.g. VITE_GOOGLE_CLIENT_ID for opt-in cloud saves — never commit it)
.env.local
.env.*.local

# test + tooling artifacts
playwright-report/
test-results/
coverage/
*.log

# editor / OS noise
.DS_Store
.vscode/
